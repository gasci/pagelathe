---
import type { z } from "zod";
import type { propsSchema } from "./schema.js";

type Props = z.infer<typeof propsSchema>;
const { heading, subhead, items } = Astro.props as Props;
---

<section class="mx-auto max-w-6xl px-6 py-24" data-section="features">
  <div class="max-w-2xl">
    <h2 class="text-3xl font-bold tracking-tight text-fg">{heading}</h2>
    {subhead && <p class="mt-3 text-muted">{subhead}</p>}
  </div>
  <div class="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
    {items.map((f) => (
      <div
        class={`rounded-[var(--radius)] border border-border bg-surface p-6 ${f.emphasis ? "sm:col-span-2 lg:row-span-2" : ""}`}
      >
        {f.icon && <div class="mb-3 text-2xl" aria-hidden="true">{f.icon}</div>}
        <h3 class="text-lg font-semibold text-fg">{f.title}</h3>
        <p class="mt-2 text-sm text-muted">{f.body}</p>
      </div>
    ))}
  </div>
</section>
