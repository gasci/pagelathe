---
import type { z } from "zod";
import type { propsSchema } from "./schema.js";

type Props = z.infer<typeof propsSchema>;
const { headline, subhead, cta, microcopy } = Astro.props as Props;
---

<section class="mx-auto max-w-4xl px-6 py-28 text-center" data-section="finalCta">
  <h2 class="text-3xl font-bold tracking-tight text-fg sm:text-4xl">{headline}</h2>
  {subhead && <p class="mt-4 text-lg text-muted">{subhead}</p>}
  <div class="mt-8">
    <a href={cta.href} class="inline-flex rounded-[var(--radius)] bg-primary px-6 py-3 text-sm font-semibold text-on-primary hover:opacity-90">
      {cta.label}
    </a>
  </div>
  {microcopy && <p class="mt-4 text-xs text-muted">{microcopy}</p>}
</section>
