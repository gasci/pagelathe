# CLAUDE.md

@AGENTS.md

## Claude Code specifics

- This repo's authoritative agent guidance lives in **AGENTS.md** (imported above) — follow it.
- Design specs and implementation plans live under `docs/superpowers/` (`specs/` and `plans/`).
  This project follows the superpowers workflow (brainstorm → spec → plan → subagent-driven
  execution); continue that flow for new work.
- Always commit with `git commit -s` (DCO) and Conventional Commit messages. Run the
  golden-path commands in AGENTS.md before claiming work is done.
